import Splash from './splash.svelte'

describe('Splash', () => {
  it('mounts', () => {
    cy.mount(Splash)
  });

  describe('on member sign in', () => {
    it('shows error modal if nonmember', () => {});
    it('shows ', () => {});
    it('', () => {});
    it('', () => {});
    it('', () => {});
  });
})
